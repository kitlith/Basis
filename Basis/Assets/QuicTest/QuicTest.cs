using UnityEngine;
using Dummy.Quic;

public class QuicTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //Sample
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
